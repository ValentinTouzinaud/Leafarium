{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <div class="formRegister">
        <h1 class="h3 mb-3 font-weight-normal"> Bienvenue </h1>

        {{ form_start(registrationForm) }}
            {{ form_row(registrationForm.username, { 'attr': {'class': 'form-control col-sm-3'} }) }}
            {{ form_row(registrationForm.Password, { 'attr': {'class': 'form-control col-sm-3'} }) }}
            {{ form_row(registrationForm.agreeTerms, { 'attr': {'class': 'form-control2 col-sm-3'} }) }}

            <button type="submit" class="btnLogin"> Register </button>
        {{ form_end(registrationForm) }}
    </div>
{% endblock %}
